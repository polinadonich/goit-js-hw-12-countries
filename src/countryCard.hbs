<div class = "text-country">
    <h2>{{name}}</h2>
    <img src="{{flag}}" alt="flag" width="360">
    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>

    <p>Language:</p>

    <ul>
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
</div>